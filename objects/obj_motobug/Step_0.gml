// Main logic
switch (state) {
	case states.falling:
		break;
	case states.moving:
		break;
	case states.turning:
		break;
}

// Update parameters
x = floor(pos_x);
y = floor(pos_y);
instance_update_hitbox();
