// Inherit the parent event
event_inherited();

// Scripts
reaction_index = player_reaction_spring;

// Variables
depth = depths.backstage;
image_speed = 0;
hor_offset = lengthdir_x(8, image_angle);
ver_offset = lengthdir_y(8, image_angle);
usable = true;
force = 10;
